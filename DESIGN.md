# Bit-Rate-Rush (BRR) — 基本設計

Pyxel 2.9.x / Python 3 で作る「ヴァンパイアサバイバーズ」系ゲームの基本設計。
想定環境: PC (Mac) で開発し、Anbernic 系ハンドヘルド (ROCKNIX / plumOS) でも動かす。

---

## 1. コンセプト

| 項目 | 内容 |
|---|---|
| 1プレイ時間 | 10分 (タイマー到達でボス → 撃破でクリア) |
| 操作 | 移動のみ (D-pad / 左スティック / WASD)。攻撃は全自動 |
| 成長 | 敵が落とす経験値ジェムを拾ってレベルアップ → 3択で武器/パッシブ強化 |
| 難度曲線 | 30秒ごとのウェーブ表で敵種・出現数が増える。5分/10分にボス |
| 目標体験 | 序盤「ギリギリ生き延びる」→ 中盤「画面を武器で埋め尽くす」→ 終盤「弾幕で敵が溶ける」 |
| 1回のセッションで完結 | メタ進行 (通貨・恒久強化) は v2 以降 |

---

## 2. 技術方針

### 2.1 画面・FPS
- **320×240, 60fps**。ハンドヘルドの 640×480 パネルに整数倍 (×2) で収まり、Pyxel 2.9.9 で動作確認済み。
- 開発中は `pyxel.perf_monitor(True)` を常時表示し、`update` の負荷を監視。

### 2.2 パフォーマンス予算 (Python は遅い前提)
Pyxel はロジックが純 Python なので **1フレームあたりのオブジェクト数**が最大の制約。

| 種類 | 同時上限 | 備考 |
|---|---|---|
| 敵 | 150 | 超えたら遠い敵を消して再スポーン (画面外リサイクル) |
| 弾/攻撃判定 | 200 | |
| 経験値ジェム | 300 | 溢れたら合体して1個の大ジェムに |
| パーティクル | 100 | 演出。負荷時は最初に切る |

実装ルール:
- **オブジェクトプール**: 敵・弾・ジェムは `alive` フラグ付きリストで使い回し、毎フレーム `new` しない。
- **空間ハッシュ**: 32px グリッドの dict で近傍探索。「敵 vs 弾」「敵 vs プレイヤー」「敵 vs 敵 (押し出し)」を O(n) に。
- **距離判定は2乗で比較**、`math.sqrt` / `math.hypot` を判定に使わない。
- 敵の追尾は `atan2` を毎フレーム呼ばず、(dx, dy) を長さで割るだけ。
- 敵同士の押し出しは 2 フレームに 1 回 (偶奇で半分ずつ)。
- 画面外 (カメラ矩形 + 32px マージン) の描画をスキップ。

### 2.3 入力
```
移動  : D-pad / GAMEPAD1_AXIS_LEFTX,LEFTY (デッドゾーン 0.25) / WASD / 矢印
決定  : GAMEPAD1_BUTTON_A / Z / Enter
戻る  : GAMEPAD1_BUTTON_B / X
ポーズ: GAMEPAD1_BUTTON_START / Esc  (quit_key は KEY_NONE にして Esc をポーズに使う)
```
`input.py` で上記を `move_x, move_y, confirm, cancel, pause` に正規化し、ゲーム側はキーを直接見ない。

### 2.4 座標系
- ワールド座標は float。マップは **無限スクロール** (タイルマップ 1 枚をモジュロで敷き詰め)。
- `pyxel.camera(px - 160, py - 120)` でプレイヤー中心。UI 描画前に `pyxel.camera()` でリセット。

### 2.5 アセット
- `assets/game.pyxres` 1 ファイル。イメージバンク 0 = キャラ/敵/弾、1 = タイル/UI、2 = 予備。
- スプライトは 8×8 (小型敵・弾) と 16×16 (プレイヤー・中型敵・ボス 32×32)。
- 効果音は `pyxel.sounds[]`、BGM は `pyxel.musics[0]`。ゲーム中の SE は 3 チャンネル (ch1〜3) をローテーション、ch0 は BGM 固定。

---

## 3. アーキテクチャ

### 3.1 ファイル構成
```
pyxel_survivors/
  main.py            # pyxel.init / run。Game を生成して update/draw を委譲
  game.py            # シーン管理 (Title / Play / LevelUp / Pause / Result)
  scenes/
    title.py
    play.py          # メインループ。以下のシステムを順番に呼ぶ
    levelup.py       # 3 択オーバーレイ (Play を止めて重ねる)
    result.py
  core/
    input.py         # 入力正規化
    pool.py          # オブジェクトプール
    spatial.py       # 空間ハッシュ
    camera.py
    rng.py           # シード付き乱数 (再現性/デバッグ用)
  entities/
    player.py
    enemy.py         # Enemy クラス + 種別テーブル
    bullet.py        # 攻撃判定の共通型
    pickup.py        # ジェム / 回復 / 磁石
    particle.py
  systems/
    spawner.py       # ウェーブ表に従って敵を出す
    weapons.py       # 武器ごとの発射ロジック
    upgrades.py      # レベルアップ候補の抽選
    collision.py
  data/
    enemies.py       # 敵定義 (dict のテーブル)
    weapons.py       # 武器定義 + レベル別パラメータ
    waves.py         # 時間 → 出現テーブル
  assets/
    game.pyxres
  ui/
    hud.py           # HP / XP バー / タイマー / キル数
    menu.py          # カーソル付きリスト
```

### 3.2 メインループ (Play シーン)
```
update():
  input.poll()
  if paused / levelup: return  (オーバーレイ側が処理)
  timer += 1/60
  spawner.update(timer)          # 敵追加
  player.update()                # 移動
  weapons.update()               # クールダウン → 弾生成
  enemies.update()               # 追尾 + 押し出し (空間ハッシュ再構築)
  bullets.update()               # 移動・寿命
  collision.bullets_vs_enemies() # ダメージ, ノックバック, 撃破 → ジェム生成
  collision.enemies_vs_player()  # 接触ダメージ (無敵時間 0.5s)
  pickups.update()               # 磁石範囲で吸い寄せ → 取得で XP
  if player.xp >= next: push LevelUp scene
  particles.update()
  if player.hp <= 0: push Result

draw():
  camera を設定 → 地面タイル → ジェム → 敵 → プレイヤー → 弾 → パーティクル
  camera リセット → HUD
```

### 3.3 データ駆動
敵・武器・ウェーブは Python の dict/list テーブルで定義し、コードを触らずバランス調整できるようにする。

```python
# data/enemies.py
ENEMIES = {
  "bat":    dict(hp=3,  spd=1.2, dmg=3,  xp=1, size=8,  sprite=(0, 0)),
  "zombie": dict(hp=10, spd=0.6, dmg=6,  xp=2, size=8,  sprite=(8, 0)),
  "ghost":  dict(hp=6,  spd=1.6, dmg=4,  xp=2, size=8,  sprite=(16,0), phase=True),
  "brute":  dict(hp=60, spd=0.5, dmg=15, xp=10, size=16, sprite=(0, 8)),
  "boss1":  dict(hp=1500, spd=0.7, dmg=25, xp=100, size=32, sprite=(0,16), boss=True),
}

# data/waves.py  — (開始秒, 敵種, 秒間出現数, 同時上限)
WAVES = [
  (  0, "bat",    2, 30),
  ( 30, "zombie", 1, 20),
  ( 60, "bat",    4, 60),
  ( 90, "ghost",  2, 30),
  (150, "brute",  0.3, 5),
  (300, "boss1",  0, 1),   # 5分ボス
  ...
]
```

---

## 4. ゲームシステム詳細

### 4.1 プレイヤー
- HP 100、移動速度 1.5px/f、拾得半径 24px。
- 接触ダメージ後 30f 無敵 (点滅)。
- レベル必要 XP: `5 + level * 3` (序盤テンポ重視、後半は敵の XP が増えるので相殺)。

### 4.2 武器 (初期 6 種、最大 4 スロット)
| 武器 | 挙動 | レベルで伸びる |
|---|---|---|
| ナイフ | 向いている方向へ直進弾 | 数, ダメージ, 貫通 |
| 魔法弾 | 最寄りの敵へ自動照準 | 数, ダメージ, 連射 |
| 斧 | 山なりに投げる。落下中も判定 | 数, 範囲 |
| 聖水 | 地面にダメージ床を生成 | 範囲, 持続 |
| 衛星 | プレイヤー周囲を周回 | 数, 回転速度 |
| 雷 | ランダムな敵に落雷 | 数, ダメージ |

各武器 Lv1〜8。Lv8 で「進化」(条件: 特定パッシブ所持) は v2。

### 4.3 パッシブ (最大 4 スロット)
攻撃力 +10%/Lv、クールダウン −8%/Lv、移動速度 +10%/Lv、最大 HP +20/Lv、拾得半径 +25%/Lv、弾数 +1 (Lv1 のみ)。

### 4.4 レベルアップ 3 択の抽選
1. 所持武器/パッシブで Lv 上限未満のもの → 重み 3
2. 未所持で空きスロットがあるもの → 重み 2
3. 全部埋まって上限なら「回復 / 金貨」を出す
重複なしで 3 つ引く。時間停止し、オーバーレイで選択。

### 4.5 敵 AI
- 基本: プレイヤーへ直進。`ghost` は敵同士の押し出しを無視。
- スポーン位置: カメラ矩形の外周 +16〜48px のランダム点。
- プレイヤーから 400px 以上離れた敵は消して再スポーン (追いつけない敵を溜めない)。
- ボス: 専用 HP バー、周期的な突進 + 取り巻き召喚。撃破でクリア。

### 4.6 ドロップ
- 撃破時に XP ジェム (青 1 / 緑 5 / 赤 20)。
- 2% で回復 (HP+30)、1% で磁石 (全ジェム吸引)。
- ジェムが上限に達したら、最も遠いジェムの値を最も近いジェムに合算して削除。

### 4.7 HUD
```
上: XP バー (画面幅いっぱい) + Lv 表示
左上: HP バー
中央上: タイマー mm:ss
右上: キル数
左下: 武器アイコン×4 + Lv, パッシブアイコン×4 + Lv
```

---

## 5. シーン遷移
```
Title ──A──▶ Play ──(XP満)──▶ LevelUp ──選択──▶ Play
                │
                ├──(HP0 / ボス撃破)──▶ Result ──A──▶ Title
                │
                └──START──▶ Pause ──START──▶ Play
                                  └──B長押し──▶ Title
```
`game.py` はシーンスタック。LevelUp / Pause は Play の上に重ねて描画するため、Play の `draw()` を呼んだ後に半透明 (`pyxel.dither(0.5)` で黒矩形) を重ねる。

---

## 6. 実装ロードマップ

| Step | 内容 | 完了条件 |
|---|---|---|
| 1 | 骨組み: main / Play シーン / 入力 / カメラ / 無限床 | パッドで歩き回れる |
| 2 | 敵 1 種 + スポーン + 追尾 + 押し出し + 接触ダメージ | 150 体で 60fps 維持 |
| 3 | 武器 2 種 (ナイフ・魔法弾) + 空間ハッシュ衝突 | 敵を倒せる |
| 4 | ジェム / XP / レベルアップ 3 択 | 成長ループが回る |
| 5 | 武器・パッシブを全種追加、ウェーブ表 | 10 分通しで遊べる |
| 6 | ボス / リザルト / タイトル / SE / BGM | 1 本のゲームとして完結 |
| 7 | ハンドヘルド実機で負荷計測 → 上限値・パーティクル調整 | 実機 60fps (無理なら 30fps に落として fps 非依存の速度定数へ) |

**Step 2 の負荷計測が最重要**。ここで実機の限界がわかってから敵数・弾数の上限を確定する。

---

## 7. 未決定事項 (実装前に決めたい)
1. 実機のターゲットが 60fps か 30fps か (30fps なら速度定数を 2 倍、`fps=30` で CPU に余裕)
2. キャラ絵は自作か、8×8 の単色シルエット + 色違いで始めるか (プロトタイプは後者推奨)
3. メタ進行 (通貨・恒久強化・キャラ解放) を v1 に入れるか
